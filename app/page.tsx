'use client'

import { useState } from 'react'
import {
  Activity,
  ArrowUpRight,
  BarChart3,
  Bot,
  Check,
  ChevronDown,
  CircleHelp,
  Code2,
  Command,
  FileText,
  Gauge,
  Globe2,
  Layers3,
  Menu,
  Search,
  Server,
  Settings2,
  ShieldCheck,
  Sparkles,
  Target,
  X,
  Zap,
} from 'lucide-react'

const prompts = ['Where are competitors outranking us in AI answers?', 'Draft an llms.txt for product pages', 'Why does AI trust competitors over us?']
const engines = [
  { name: 'ChatGPT', score: '42.8%', color: 'bg-emerald-400' },
  { name: 'Claude', score: '38.4%', color: 'bg-amber-300' },
  { name: 'Perplexity', score: '31.9%', color: 'bg-sky-300' },
  { name: 'Gemini', score: '27.5%', color: 'bg-zinc-500' },
]
const plans = [
  { name: 'Free', price: '$0', target: 'Quick audits & trial', limit: 'Up to 50 pages', features: ['Basic Technical SEO audit', 'Citable Visibility Score', '1 Website', 'Manual recommendations preview'] },
  { name: 'Individuals', price: '$49', target: 'Solo founders & site owners', limit: 'Up to 500 pages', features: ['1 Website', '5 Tracked AI Visibility prompts', 'Basic Content Studio editor', 'Google Search Console sync', 'Weekly automated audits'] },
  { name: 'Business', price: '$80', target: 'Growing brands & e-commerce stores', limit: 'Up to 2,500 pages', featured: true, features: ['5 Websites', '25 Tracked AI Visibility prompts', 'Full Citation Graphing', 'Product Schema Completeness engine', 'Competitor intelligence tracking', 'Automated JSON-LD fix generator'] },
  { name: 'Enterprise', price: '$250', target: 'Agencies & large enterprise catalogs', limit: '10,000+ pages', features: ['Unlimited Websites', 'Custom AI agent prompt tracking', 'Dedicated MCP server integration', 'White-label reporting', 'Priority crawl queues', 'Dedicated account manager'] },
]

function Badge({ children, tone = 'neutral' }: { children: React.ReactNode; tone?: 'neutral' | 'success' | 'warning' }) {
  return <span className={`inline-flex items-center gap-1.5 rounded-md border px-2 py-1 font-mono text-[10px] uppercase tracking-[0.12em] ${tone === 'success' ? 'border-emerald-400/25 bg-emerald-400/10 text-emerald-300' : tone === 'warning' ? 'border-amber-300/25 bg-amber-300/10 text-amber-200' : 'border-white/10 bg-white/[0.04] text-zinc-400'}`}>{children}</span>
}

function MetricCard({ label, value, delta, icon: Icon }: { label: string; value: string; delta?: string; icon: React.ElementType }) {
  return <div className="metric-card group"><div className="flex items-center justify-between text-zinc-500"><span className="text-xs">{label}</span><Icon className="size-4 transition-colors group-hover:text-emerald-300" /></div><div className="mt-4 flex items-end justify-between"><strong className="font-mono text-2xl tracking-tight text-zinc-100">{value}</strong>{delta && <span className="font-mono text-xs text-emerald-300">{delta}</span>}</div></div>
}

function AppShell({ children, mobileOpen, setMobileOpen }: { children: React.ReactNode; mobileOpen: boolean; setMobileOpen: (v: boolean) => void }) {
  return <div className="min-h-screen bg-[#090a0c] text-zinc-100"><aside className={`fixed inset-y-0 left-0 z-30 flex w-60 flex-col border-r border-white/10 bg-[#0c0d0f] p-4 transition-transform lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'}`}><div className="flex items-center justify-between px-2"><div className="flex items-center gap-2.5"><div className="grid size-7 place-items-center rounded-md bg-emerald-300 text-[#090a0c]"><Zap className="size-4 fill-current" /></div><span className="text-sm font-semibold tracking-tight">citable</span></div><button className="lg:hidden" onClick={() => setMobileOpen(false)} aria-label="Close navigation"><X className="size-4" /></button></div><div className="mt-9 px-2 text-[10px] font-medium uppercase tracking-[0.18em] text-zinc-600">Workspace</div><nav className="mt-3 flex flex-col gap-1 text-sm"><a className="nav-item nav-active" href="#dashboard"><Gauge className="size-4" />Overview</a><a className="nav-item" href="#insights"><BarChart3 className="size-4" />AEO Insights</a><a className="nav-item" href="#studio"><FileText className="size-4" />Content Studio</a><a className="nav-item" href="#health"><ShieldCheck className="size-4" />Site Health</a></nav><div className="mt-8 px-2 text-[10px] font-medium uppercase tracking-[0.18em] text-zinc-600">Manage</div><nav className="mt-3 flex flex-col gap-1 text-sm"><a className="nav-item" href="#pricing"><Layers3 className="size-4" />Plans & billing</a><a className="nav-item" href="#integrations"><Settings2 className="size-4" />Integrations</a></nav><div className="mt-auto rounded-lg border border-white/10 bg-white/[0.03] p-3"><div className="flex items-center gap-2 text-xs"><Server className="size-4 text-emerald-300" /><span>VPS Cloud Hosting</span></div><p className="mt-2 text-[11px] leading-relaxed text-zinc-500">Automated deployment engine coming soon.</p><Badge tone="warning">Roadmap</Badge></div><div className="mt-3 flex items-center gap-2 border-t border-white/10 px-2 pt-3 text-xs text-zinc-400"><div className="grid size-7 place-items-center rounded-full bg-zinc-800 font-mono text-[10px]">AC</div><span>Acme Co.</span><ChevronDown className="ml-auto size-3" /></div></aside><div className="lg:pl-60">{children}</div></div>
}

function AgentHeader({ setCommandOpen }: { setCommandOpen: (v: boolean) => void }) {
  const [prompt, setPrompt] = useState('')
  const [submitted, setSubmitted] = useState(false)
  return <section className="border-b border-white/10 bg-[#0c0d0f] px-5 py-5 sm:px-8"><div className="mx-auto max-w-[1400px]"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="text-xs text-zinc-500">Wednesday, September 12, 2026</span><Badge tone="success"><span className="size-1.5 rounded-full bg-emerald-300" />Agent live</Badge></div><h1 className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">Good morning, Acme.</h1><p className="mt-1 text-sm text-zinc-500">Your brand is being cited in <span className="text-zinc-300">35.1%</span> of relevant AI answers.</p></div><button onClick={() => setCommandOpen(true)} className="command-trigger"><Command className="size-3.5" /> Search workspace <kbd>⌘ K</kbd></button></div><div className="mt-6 max-w-4xl"><div className="agent-input"><Bot className="size-4 text-emerald-300" /><input value={prompt} onChange={(e) => { setPrompt(e.target.value); setSubmitted(false) }} onKeyDown={(e) => { if (e.key === 'Enter' && !e.nativeEvent.isComposing && e.keyCode !== 229) setSubmitted(true) }} placeholder="Ask the agent about your AI visibility..." /><button onClick={() => setSubmitted(true)} aria-label="Run agent" className="grid size-8 place-items-center rounded-md bg-emerald-300 text-[#090a0c] transition-transform hover:scale-105"><ArrowUpRight className="size-4" /></button></div>{submitted && <div className="mt-2 border-l-2 border-emerald-300 px-3 py-2 text-xs text-zinc-400">Agent context synced across GA4 and Search Console. I&apos;ll map the answer to your tracked sources.</div>}<div className="mt-3 flex flex-wrap gap-2">{prompts.map((item) => <button key={item} onClick={() => setPrompt(item)} className="prompt-chip">{item}</button>)}</div></div></div></section>
}

function TrendGraph() { return <div className="mt-5 h-40 overflow-hidden rounded-md border border-white/10 bg-[#0b0c0e] p-3"><svg viewBox="0 0 700 140" className="h-full w-full" preserveAspectRatio="none" aria-label="Visibility score trend"><defs><linearGradient id="fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="#6ee7b7" stopOpacity=".18" /><stop offset="1" stopColor="#6ee7b7" stopOpacity="0" /></linearGradient></defs><path d="M0 112 C55 103 67 106 110 89 S175 103 213 81 S270 80 305 92 S355 76 393 62 S450 83 482 61 S535 53 565 42 S620 45 700 18 L700 140 L0 140Z" fill="url(#fill)" /><path d="M0 112 C55 103 67 106 110 89 S175 103 213 81 S270 80 305 92 S355 76 393 62 S450 83 482 61 S535 53 565 42 S620 45 700 18" fill="none" stroke="#6ee7b7" strokeWidth="2" /></svg><div className="flex justify-between font-mono text-[9px] text-zinc-600"><span>JUN 12</span><span>JUL 12</span><span>AUG 12</span><span>SEP 12</span></div></div> }

function Insights() { return <div id="insights" className="grid gap-4 xl:grid-cols-[1.45fr_1fr]"><div className="panel"><div className="flex items-start justify-between"><div><div className="section-kicker">AEO insights</div><h2 className="panel-title">Visibility over time</h2></div><Badge tone="success">+11.9% vs last month</Badge></div><div className="mt-5 flex items-end gap-3"><span className="font-mono text-4xl tracking-tight">35.1%</span><span className="mb-1 text-xs text-emerald-300">↑ 3.7 pts</span></div><TrendGraph /></div><div className="panel"><div className="flex items-start justify-between"><div><div className="section-kicker">Engine breakdown</div><h2 className="panel-title">Where you show up</h2></div><button className="icon-button" aria-label="Help"><CircleHelp className="size-4" /></button></div><div className="mt-6 flex flex-col gap-4">{engines.map((engine) => <div key={engine.name}><div className="mb-1.5 flex justify-between text-xs"><span className="text-zinc-400">{engine.name}</span><span className="font-mono text-zinc-200">{engine.score}</span></div><div className="h-1.5 rounded-full bg-zinc-800"><div className={`h-full rounded-full ${engine.color}`} style={{ width: engine.score }} /></div></div>)}</div></div></div> }

function CitationFlow() { return <div className="panel"><div className="flex items-center justify-between"><div><div className="section-kicker">Citation graph</div><h2 className="panel-title">How AI finds your brand</h2></div><Badge>Last crawled 14m ago</Badge></div><div className="mt-6 grid gap-2 sm:grid-cols-3"><div className="flow-node"><Globe2 className="size-4 text-zinc-500" /><span>Source domain</span><strong>acme.com</strong></div><div className="flow-arrow">←</div><div className="flow-node"><Bot className="size-4 text-emerald-300" /><span>AI response</span><strong>1,248 answers</strong></div><div className="flow-arrow hidden sm:block">←</div><div className="flow-node"><Target className="size-4 text-emerald-300" /><span>Brand cited</span><strong>35.1% share</strong></div></div></div> }

function Studio() { const [preview, setPreview] = useState('Search Appearance'); const [modal, setModal] = useState(false); return <div id="studio" className="grid gap-4 xl:grid-cols-[1fr_1.3fr]"><div className="panel"><div className="section-kicker">Content studio</div><div className="flex items-center justify-between"><h2 className="panel-title">Product page readiness</h2><button onClick={() => setModal(true)} className="text-xs text-emerald-300 hover:text-emerald-200">Fix issues <ArrowUpRight className="ml-1 inline size-3" /></button></div><div className="mt-6 flex items-center gap-6"><div className="gauge"><span>84</span><small>/100</small></div><div><Badge tone="success">Ready to publish</Badge><p className="mt-2 max-w-[220px] text-xs leading-relaxed text-zinc-500">Strong structure. Resolve the remaining source signals to improve citation confidence.</p></div></div><div className="mt-7 flex flex-col gap-3">{[['Missing Product schema', 'High priority'], ['Source citation gaps', 'Medium priority'], ['Title is AI-readable', 'Passed']].map(([label, status]) => <div className="flex items-center justify-between border-t border-white/10 pt-3 text-xs" key={label}><span className="flex items-center gap-2 text-zinc-400"><span className={`size-1.5 rounded-full ${status === 'Passed' ? 'bg-emerald-300' : 'bg-amber-300'}`} />{label}</span><span className="font-mono text-[10px] text-zinc-600">{status}</span></div>)}</div>{modal && <div className="modal-backdrop" onClick={() => setModal(false)}><div className="modal-card" onClick={(e) => e.stopPropagation()}><div className="flex items-center justify-between"><h3 className="font-medium">Fix Product schema</h3><button onClick={() => setModal(false)}><X className="size-4 text-zinc-500" /></button></div><p className="mt-3 text-xs leading-relaxed text-zinc-500">Generate a validated JSON-LD block for this page and copy it to your CMS.</p><button onClick={() => setModal(false)} className="mt-5 w-full rounded-md bg-emerald-300 px-3 py-2 text-xs font-semibold text-[#090a0c]">Generate fix</button></div></div>}</div><div className="panel"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="section-kicker">Markup preview</div><h2 className="panel-title">Structured content layers</h2></div><div className="tab-list">{['Search Appearance', 'FAQ Schema', 'JSON-LD Markup'].map((item) => <button key={item} onClick={() => setPreview(item)} className={preview === item ? 'tab-active' : ''}>{item}</button>)}</div></div><div className="code-preview mt-6">{preview === 'Search Appearance' && <><div className="text-[10px] text-zinc-600">SEARCH RESULT PREVIEW</div><div className="mt-4 text-sm text-sky-300">Acme — Better tools for modern teams</div><p className="mt-1 text-xs leading-relaxed text-zinc-500">Make work simpler with a connected workspace that helps your team move from idea to impact.</p></>}{preview === 'FAQ Schema' && <><div className="text-[10px] text-zinc-600">FAQ SCHEMA</div><pre className="mt-4 whitespace-pre-wrap font-mono text-[11px] leading-relaxed text-zinc-400">{`{\n  "@type": "FAQPage",\n  "mainEntity": [...]\n}`}</pre></>}{preview === 'JSON-LD Markup' && <><div className="text-[10px] text-zinc-600">JSON-LD MARKUP</div><pre className="mt-4 whitespace-pre-wrap font-mono text-[11px] leading-relaxed text-emerald-200/70">{`{\n  "@context": "schema.org",\n  "@type": "Product",\n  "name": "Acme Platform"\n}`}</pre></>}</div></div></div> }

function Health() { return <div id="health" className="panel"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="section-kicker">Site health & technical AEO</div><h2 className="panel-title">Your technical foundation is healthy</h2><p className="mt-1 text-xs text-zinc-500">Combined SEO + AEO crawlability score</p></div><div className="text-right"><div className="font-mono text-3xl">88.4<span className="text-sm text-zinc-600">/100</span></div><Badge tone="success">Good standing</Badge></div></div><div className="mt-6 grid gap-2 md:grid-cols-3">{[['Crawlability', '94.1', 'bg-emerald-300'], ['AI readability', '86.7', 'bg-emerald-300'], ['Critical errors', '03', 'bg-amber-300']].map(([name, value, color]) => <div className="audit-row" key={name}><span className="text-xs text-zinc-500">{name}</span><strong className="font-mono text-sm">{value}</strong><span className={`h-1 w-12 rounded-full ${color}`} /></div>)}</div></div> }

function IntegrationTray() { return <div id="integrations" className="integration-tray"><span className="text-[10px] uppercase tracking-[0.16em] text-zinc-600">Connected ecosystem</span>{['GA4', 'Search Console', 'Shopify', 'Notion', 'Slack', 'MCP'].map((name, i) => <div className="integration" key={name}><span className={`integration-dot ${i < 2 ? 'bg-emerald-300' : 'bg-zinc-600'}`} />{name}</div>)}</div> }

function Pricing() { const [annual, setAnnual] = useState(false); return <section id="pricing" className="border-t border-white/10 bg-[#0c0d0f] px-5 py-20 sm:px-8"><div className="mx-auto max-w-[1400px]"><div className="flex flex-wrap items-end justify-between gap-5"><div><div className="section-kicker">Plans & billing</div><h2 className="mt-2 text-3xl font-semibold tracking-tight">A clearer view of your AI visibility.</h2><p className="mt-2 text-sm text-zinc-500">Start small. Scale when your citations compound.</p></div><div className="toggle"><button className={!annual ? 'selected' : ''} onClick={() => setAnnual(false)}>Monthly</button><button className={annual ? 'selected' : ''} onClick={() => setAnnual(true)}>Annual <span>save 20%</span></button></div></div><div className="mt-9 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{plans.map((plan) => <div className={`price-card ${plan.featured ? 'featured' : ''}`} key={plan.name}>{plan.featured && <div className="popular">Most popular</div>}<div className="flex items-start justify-between"><h3 className="font-medium">{plan.name}</h3><ArrowUpRight className="size-4 text-zinc-600" /></div><div className="mt-6 font-mono text-3xl">{annual && plan.price !== '$0' ? plan.price === '$49' ? '$39' : plan.price === '$80' ? '$64' : '$200' : plan.price}<span className="font-sans text-xs text-zinc-600">/mo</span></div><p className="mt-2 min-h-8 text-xs text-zinc-500">{plan.target}</p><Badge>{plan.limit}</Badge><div className="mt-6 flex flex-col gap-3 border-t border-white/10 pt-5">{plan.features.map((feature) => <div className="flex gap-2 text-xs text-zinc-400" key={feature}><Check className="mt-0.5 size-3.5 shrink-0 text-emerald-300" />{feature}</div>)}</div><button className={`mt-8 w-full rounded-md px-3 py-2.5 text-xs font-semibold transition-colors ${plan.featured ? 'bg-emerald-300 text-[#090a0c] hover:bg-emerald-200' : 'border border-white/15 bg-white/[0.03] text-zinc-200 hover:bg-white/[0.08]'}`}>{plan.name === 'Free' ? 'Start free' : 'Choose plan'}</button></div>)}</div></div></section> }

export default function Page() { const [mobileOpen, setMobileOpen] = useState(false); const [commandOpen, setCommandOpen] = useState(false); return <AppShell mobileOpen={mobileOpen} setMobileOpen={setMobileOpen}><div className="min-h-screen"><div className="flex h-14 items-center justify-between border-b border-white/10 bg-[#0c0d0f] px-5 lg:hidden"><button onClick={() => setMobileOpen(true)} aria-label="Open navigation"><Menu className="size-5" /></button><span className="text-sm font-semibold">citable</span><button onClick={() => setCommandOpen(true)} aria-label="Open search"><Search className="size-4" /></button></div><AgentHeader setCommandOpen={setCommandOpen} /><main id="dashboard" className="mx-auto max-w-[1400px] px-5 py-6 sm:px-8"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Visibility score" value="35.1%" delta="+11.9%" icon={Target} /><MetricCard label="Tracked prompts" value="128" delta="+14 this week" icon={Sparkles} /><MetricCard label="Citations found" value="1,248" delta="+8.6%" icon={Code2} /><MetricCard label="Site health" value="88.4" delta="Good" icon={Activity} /></div><div className="mt-4 grid gap-4"><Insights /><CitationFlow /><Studio /><Health /></div><IntegrationTray /><div className="mt-5 flex flex-wrap items-center justify-between gap-4 border border-emerald-300/20 bg-emerald-300/[0.04] px-4 py-3 text-xs"><div className="flex items-center gap-3"><Server className="size-4 text-emerald-300" /><span className="text-zinc-300">Citable VPS Cloud Hosting</span><span className="hidden text-zinc-600 sm:inline">— High-performance automated deployment engine coming soon.</span></div><Badge tone="warning">Future offering</Badge></div><div className="mt-6 flex flex-wrap gap-3 border-t border-white/10 pt-5"><a className="download-link" href="/citable-antigravity-ui-brief.md" download><FileText className="size-4" />Download Antigravity brief</a><a className="download-link" href="/citable-antigravity-preview.html" target="_blank" rel="noreferrer"><ArrowUpRight className="size-4" />Open visual preview</a></div></main><Pricing /></div>{commandOpen && <div className="modal-backdrop" onClick={() => setCommandOpen(false)}><div className="command-modal" onClick={(e) => e.stopPropagation()}><div className="flex items-center gap-2 border-b border-white/10 px-4 py-3"><Search className="size-4 text-zinc-500" /><input autoFocus placeholder="Search workspace..." className="flex-1 bg-transparent text-sm outline-none" /><kbd>ESC</kbd></div><div className="p-2 text-xs text-zinc-500"><div className="command-result"><BarChart3 className="size-4" />Open AEO Insights <span>↵</span></div><div className="command-result"><FileText className="size-4" />Go to Content Studio <span>↵</span></div></div></div></div>}</AppShell> }
